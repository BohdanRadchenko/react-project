// import React from 'react';
// import { Route, Redirect } from 'react-router-dom'

// const isRouting = ({signIn, ...props}) => (
//    signIn ? <Render {...props} /> : <Redirect to= '/signup'/>
// )

// const mapStateToProps = {
//    signIn:
// }
